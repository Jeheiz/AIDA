// This file is for helper functions that can be used across the app.

const helpers = {
  // Example helper function
  formatDate: (date) => {
    return new Date(date).toLocaleDateString();
  },
};

export default helpers;
